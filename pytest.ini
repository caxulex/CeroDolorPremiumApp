[pytest]
testpaths = tests
addopts = -q
markers =
	integration: integration tests that may start local servers or use network on localhost
	e2e: end-to-end UI/browser tests (Playwright optional)
pythonpath = .